<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:tools="http://schemas.android.com/tools">

    <uses-permission android:name="android.permission.ACCESS_NETWORK_STATE" />
    <uses-permission android:name="android.permission.INTERNET"></uses-permission>

    <application
        android:usesCleartextTraffic="true"
        android:allowBackup="true"
        android:dataExtractionRules="@xml/data_extraction_rules"
        android:fullBackupContent="@xml/backup_rules"
        android:icon="@drawable/logo"
        android:label="@string/app_name"
        android:supportsRtl="true"
        android:theme="@style/AppTheme"
        tools:targetApi="31">

        <activity android:name=".RegisterActivity"></activity>

        <activity
            android:name=".MainActivity"
            android:exported="true"
            android:theme="@style/SplashTheme">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>

            <meta-data
                android:name="android.app.lib_name"
                   android:value="" />
        </activity>

        <activity android:name=".MainActivityAnime"
            android:exported="false"
            />

        <activity
            android:name=".CapitulosAnime"
            android:exported="false" />

        <activity
            android:name=".AnimeActivity"
            android:exported="false"
            android:parentActivityName=".MainActivityAnime"
            />

        <activity
            android:name=".DirectoryFragment"
            android:exported="false" />

        <activity
            android:name=".CapitulosManga"
            android:exported="false" />

        <activity
            android:name=".Soundtracks"
            android:exported="false" />

        <activity android:name=".VideoPlayer"
            android:exported="false"
            android:parentActivityName=".CapitulosAnime"
            />

        <activity android:name=".MangaReader"
            android:exported="false"
            android:parentActivityName=".CapitulosManga"
            />


    </application>

</manifest>